# Paper Notes

`model_details.tex` is a standalone LaTeX description of the mathematical
model implemented in this repository. It can be compiled directly or copied
into a larger manuscript with `\input{paper/model_details.tex}` after removing
the standalone preamble.
